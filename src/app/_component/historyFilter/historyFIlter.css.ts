import { style } from "@vanilla-extract/css";

export const historyFilterwrap = style({
  display: "flex",
  gap: 30,
});

export const btnWrap = style({
  marginLeft: "auto",
  display: "flex",
  gap: 10,
});
